<!DOCTYPE html>
<html>
    <head>
        <title>Villains</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link href="../styles.css" rel="stylesheet">

    </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light nav justify-content-end">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">SQUADS</a>
            <div>
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="nav-link active" aria-current="page" href="/villains">VILLAINS</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/heroes">SWITCH SIDES</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container-fluid">
        <div class="row">
            {{#if squads}}
                {{#each squads}}
                    <li><a href="/villainssquad/new/{{id}}">{{name}}</a></li> <!--add route-->
                {{/each}}
                <li><a href="/villainssquad/new">Add a Squad</a></li> <!--add route-->
            {{else}}
                <li><a href="/villainssquad/new">Add a Squad</a></li> <!--add route-->
            {{/if}}
        </div>
        <div class="row">
            <h1>{{squad.name}}</h1>
            {{#if heroes}}
                {{#each heroes}}
                    <p>{{name}}<a href="#">READ MORE</a></p> <!--add route-->
                {{/each}}
            {{else}}
                <p>No Villains in this squad!</p>
            {{/if}}
        </div>
    </div>
    <a href="/villainssquad/delete">delete all squads and villains</a><br>
    <footer>
        <hr>
        <p><img src='/images/' alt="copyright">2022 Squads</p>
    </footer>
    </body>
</html>