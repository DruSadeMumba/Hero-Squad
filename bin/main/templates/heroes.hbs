<!DOCTYPE html>
<html>
    <head>
        <title>Heroes</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.8.1/font/bootstrap-icons.css">
        <link rel="preconnect" href="https://fonts.googleapis.com">
        <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
        <link href="https://fonts.googleapis.com/css2?family=Caveat:wght@400;500;600;700&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&family=Roboto:ital,wght@0,100;0,300;0,400;0,500;0,700;0,900;1,100;1,300;1,400;1,500;1,700;1,900&family=Sedgwick+Ave+Display&display=swap" rel="stylesheet">
        <link href="/styles/styles.css" rel="stylesheet">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light nav justify-content-end">
            <div class="container-fluid">
                <a class="navbar-brand" href="/landing">SQUADS</a>
                <div>
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/">HEROES</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/squads/new">CREATE A SQUAD</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="container-fluid text-center" id="landing">
            <div class="overlay-text">
                <div class="row " >
                    {{#if squads}}
                        <div class="col-md-6">
                            {{#each squads}}
                                <p>{{name}}<a href="/squads/{{id}}"><i class="bi bi-eyeglasses"></i></a></p>
                            {{/each}}
                        </div>
                        <div class="col-md-6">
                            {{else}}
                            <p><a href="/squads/new"><button type="button" class="btn btn-info">Add a Squad</button></a></p>
                        </div>
                    {{/if}}
                </div>
                <p><a href="/squads/delete"><button type="button" class="btn btn-danger">Delete All </button></a></p>
            </div>
        </div>
    </body>
</html>











<!--<div class="row">
                    <h2>{{squad.name}}</h2>
                    <h4>List of heroes</h4>
                    <details>
                        {{#if heroes}}
    {{#each heroes}}
                                <h5>{{name}}</h5>
                                <p><a href="/squad/{{squadId}}/heroes/{{id}}">More info...</a></p>
                            {{/each}}
{{else}}
                            <p>No Heroes in this squad!</p>
                        {{/if}}
                    </details>
                </div><br>-->