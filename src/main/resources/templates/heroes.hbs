<!DOCTYPE html>
<html>
    <head>
        <title>Heroes</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-light bg-light nav justify-content-end">
            <div class="container-fluid">
                <a class="navbar-brand" href="/">SQUADS</a>
                <div>
                    <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                        <li class="nav-item">
                            <a class="nav-link active" aria-current="page" href="/heroes">HEROES</a>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/villains">SWITCH SIDES</a>
                        </li>
                    </ul>
                </div>
            </div>
        </nav>
        <div class="container-fluid">
            <div class="row">
                {{#if squads}}
                    {{#each squads}}
                        <li><a href="/heroessquad/new/{{id}}">{{name}}</a></li> <!--add route-->
                    {{/each}}
                    <li><a href="/heroessquad/new">Add a Squad</a></li> <!--add route-->
                {{else}}
                    <li><a href="/heroessquad/new">Add a Squad</a></li> <!--add route-->
                {{/if}}
            </div>
            <div class="row">
                <h1>{{squad.name}}</h1>
                {{#if heroes}}
                    {{#each heroes}}
                        <p>{{name}}<a href="#">READ MORE</a></p> <!--add route-->
                    {{/each}}
                {{else}}
                    <p>No Heroes in this squad!</p>
                {{/if}}
            </div>
        </div>
        <footer>
        <hr>
            <p><img src='/images/' alt="copyright">2022 Squads</p>
        </footer>
    </body>
</html>
