<!DOCTYPE html>
<html>
    <head>
        <title>Squads!!</title>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    </head>
    <body>
        <div class="container">
            <div class="row">
                <div class="col-md-12">
                    <h1>{{squad.name}}</h1>
                    <p>Here are all the heroes listed in this squad:</p>
                    <ul>
                        {{#each heroes}}
                            <li>
                                <p><a href="/heroessquad/{{category.id}}/heroes/{{id}}">{{name}}</a></p>
                            </li>
                        {{/each}}
                    </ul>
                    <a href="/heroessquad/{{squad.id}}/edit">Edit this squad</a><br>
                    <a href="/heroessquad/{{squad.id}}/delete">DELETE this squad</a>
                </div>
            </div>
        </div>
    </body>
</html>